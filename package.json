{
  "name": "trading-server",
  "version": "1.0.0",
  "description": "trading server for caching requests and stuff",
  "main": "app.js",
  "scripts": {
    "start:ts": "ENV='dev' nodemon --watch 'src' --ignore 'src/**/*.spec.ts' --exec 'ts-node' src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "manish-baghel",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.14.31",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.11.17",
    "redis": "^3.0.2",
    "yenv": "^3.0.0"
  }
}
